# VEVE: The Resonance - Project Status
**Snapshot at commit ef49389 (2026-08-31) - lead orchestrator log**

## Living systems (all compile, all tested, all wired to the scene, all shipped on WebGL)

| Layer | Modules | Evidence |
|---|---|---|
| Ballistics | 6-DOF ProjectileBallistics (Mach drag, Magnus, Coriolis, ricochet, terminal cavity) + RealisticWeaponDefinition catalog + ZeroingSystem range-cards (crossings/PBR/battle-zero), Weapon holdover-re-ray loop, OpticCatalogBridge (real optics change sight height & click values live), AmmunitionModel | MissionScoring/Radio tests + ak74m card anchors (+4.88/-1.27 MOA) |
| Human simulation | Physiology (blood, HR Tanaka, respiration, shock), StaminaSystem, MedicalTreatment, InjurySystem; CharacterPhysics IK stubs; PlayerController signed-gravity fix (regression-tested forever) | 9 gravity + medical suites |
| Combat feel | B2 OperatorInstance multipliers -> LookController sway + speed + reload; ScopeReticleOverlay (diegetic hold marker, per-optic px/MOA); malfunction/fouling/wear loop | 299->305 gates |
| AI | Behavior trees + HTN-lite cognition + SquadManager formations + AgentLOD (Full..Statistical tick gates) + Coordinator/Bridge python seam (BehaviorCommand protocol) | AgentLOD/Cognition suites |
| Tactics (B4) | SquadMorale FSM (KIA-chain, pinning latch, abandoned wounded), EngagementReporter, CampaignEscalationModel (posture), TacticalEventHub | 29 pure tests |
| Comms (C5) | RadioNet doctrine (morale->tone->radio protocol), RadioDispatcher (clock-injectable, debrief regroup auto) | 6 tests |
| Campaign loop (C2) | MissionSession (deterministic draft->tally->score->XP->escalate), CampaignLoopController + ShotResolvedEvent bus contract, MissionScoring B7 (failure policy + difficulty XP), OperatorLegacySystem B3 (KIA->memorial->mentorship successor), CampaignDifficulty tracks, MissionContentCatalog (10 ops / 5 biomes) + MissionScheduler | MissionSession/Content/Legacy suites |
| Gear | VEVE.Gear NIJ/VPAM (multi-hit, obliquity, trauma budgets, force-open), WeaponCustomPro (scope specs, identity FNV, finishes NIR), Customization manager | committed Gear suite + agent offline run + gates |
| World | TacticalLayoutEvaluator (LOS), PropScatter, RoomFunctionGraph, Biomes, procedural generation, DoorSystem+DoorModel, Destructible, Weather/Climate/Skybox/FogVolume | layout tests |
| UX | UiFactory + HudDiegesis per death mode (Realistic = compass+vitals), AdvancedHUDLayout (9 features), InventoryUI, MainMenuFlow + Personalization 5-tab workspace + Binder, DiegeticReadout | 48 UI tests |
| Observability | In-game DebugDashboard (F12) + Editor "VEVE > Progress Dashboard" (registry validated against disk, test results, git log) | live in HEAD |
| Build/CI | native gate rule (Unity -runTests is the single source of truth; serial, locked), scene builder deterministic regen, WebGL + GitHub Pages publishing, anti-zombie quarantine (tree-vs-HEAD before commit) | every commit carries "EditMode 305/305" |

**Total: 305 EditMode tests green, WebGL 7.7 MB published, working tree clean, deployed Pages.**

## Remaining plan (ordered)

1. **C4b - Netcode transport install**: protocol layer LANDED (VEVE.Net: NetCommand fixed-layout + MissionCommandJournal single-book, NetMissionMirror with reorder-buffer parity proof, LoopbackLink frame-deterministic). REMAINING: install com.unity.netcode.gameobjects, NetworkBehaviour adapter moving the identical NetCommand struct, host-authoritative CampaignLoop hand-off, client-only PlayerController ownership, lag-comp hitscan over residual-energy math (deterministic replay).
2. **C6 - AI perception realism pass**: LANDED cd22aac as AiAcoustics + EnemyAwareness integration (hearing cone estimate, radio callout relay between elements, W3 scope-glint) - no separate CommunicationSystem shipped/needed.
3. **C6b - Squad movement AI (this tranche)**: WaypointPatrolSystem pure model: multi-leg routes (loop/once/pingpong), arrival radius, dwell, deterministic natural-path jitter (never beeline-robot), per-route speed cap; feeds TacticalAICore patrol destination and C2 extraction legs.
4. **C7 - Content pipeline to Unity assets**
   - MissionContentCatalog -> real Prefabs/ScriptableObjects generated by an Editor tool from the catalog; IconicWeaponCatalog + GearCatalog to .asset; biomes as SubScenes + Addressables
4. **C8 - VR + modding support** (VR controller seam already in OperatorInstance; modding via ScriptableObject catalogs + JSON identity)

## Engineering rules (binding for every future tranche)
- One native gate: Unity batchmode `-runTests -testPlatform EditMode`, never ad-hoc harnesses; serial runs only.
- Orchestrator owns any file a sub-agent was assigned if that agent stalls (takeover rule; verified clean 2x).
- Every feature ships with its pure-model unit tests (the last "test failure" caught by a human/agent in C5 was the test, not the code - gate stays honest).
- Commit == pushed == built == WebGL + dashboard validated against real files.


*322/322 EditMode (C6b patrol model added); last doc-vs-code audit 2026-08-31.*

*NGO transport installed: manifest 2.13.2, MissionTransportAdapter + explicit asmdef refs (autoReferenced does NOT cascade to asmdef assemblies) -> 329/329 EditMode green incl. all pre-existing suites.*

*C4c landed: NetworkGameFlow (host/client/offline authority split, NotifyShot fact API, debrief mirror symmetry) -> 334/334 EditMode.*

*C4d landed: NetworkedPlayerAvatar + NetworkTransform movement, PlayerController input authority (offline-safe rules) -> 338/338. AI scene elements remain host-authoritative; remote NPC replication = C4e.*
